export function registerHelloStudents(app) {
    // Register this plugin's Vue components here, e.g.:
    // app.component('hello-students-example', () => import('./components/Example.vue').then(m => m.default));
}
