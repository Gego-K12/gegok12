export function registerIncomeExpense(app) {
    // Register this plugin's Vue components here, e.g.:
    // app.component('income-expense-example', () => import('./components/Example.vue').then(m => m.default));
}
