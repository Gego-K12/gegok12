export function registerUpcomingHolidays(app) {
    // Register this plugin's Vue components here, e.g.:
    // app.component('upcoming-holidays-example', () => import('./components/Example.vue').then(m => m.default));
}
